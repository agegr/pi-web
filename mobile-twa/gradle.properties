# Pi Web TWA — build properties.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true

# Origin the shell opens at launch (plan decision: gradle property, HTTPS only).
# Override per build with -PPIWEB_TWA_LAUNCH_URL=https://your-origin
# (or edit this file); no code change is needed to retarget.
PIWEB_TWA_LAUNCH_URL=https://gcp-dev.stevenforai.top
