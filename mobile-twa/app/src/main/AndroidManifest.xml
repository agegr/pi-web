<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The shell itself opens the origin in the browser (Custom Tabs);
         the INTERNET permission keeps the manifest self-contained for
         tooling that inspects it. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <!-- Launcher alias: tapping the home icon starts the launcher, which
             immediately hands the origin to Custom Tabs and finishes. The
             launch URL comes from the generated @string/twa_launch_url
             resource (PIWEB_TWA_LAUNCH_URL gradle property). -->
        <activity
            android:name=".TwaLauncherActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
