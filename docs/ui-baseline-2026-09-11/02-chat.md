# pi-web 聊天功能界面基线与优化对比

记录日期：2026-09-11。本清单依据当前仓库源码确认，未对每个动态状态逐一取得运行截图；它是功能和交互基线，不等同于经过浏览器验证的视觉缺陷报告。优化方向属于建议，尚未实施。

聊天相关功能大多是同一主工作台中的空白态、消息区、输入区、顶部下拉面板、浮层和消息内嵌卡片，不是各自拥有独立 URL 的页面。以下按功能界面编号，便于优化前后使用相同状态对比。

## 功能界面清单

| 编号 | 功能界面 | 入口与当前布局 | 操作与状态 | 优化对比方向 | 源码 |
|---|---|---|---|---|---|
| C01 | 新会话首页 | 新建且无消息时，主区中央显示 π / Pi Web、更新说明入口、web/pi 版本，下方为输入器和扩展状态。 | 第一条输入后进入正常聊天布局；无模型提示由输入区承载。 | 对比增加工作目录、当前配置摘要和少量起步操作后，首次输入是否更容易。 | [ChatWindow](../../components/ChatWindow.tsx)，第 691 行 |
| C02 | 会话消息主区 | 消息列最大宽 820px，纵向滚动；桌面右侧 Minimap；输入器位于底部。 | 加载、错误、旧消息增量加载、流式回答、阶段等待、运行命令、完成。 | 对比统一加载失败恢复入口和运行状态区域后，当前状态是否更清晰。 | [ChatWindow](../../components/ChatWindow.tsx)，第 603、611、724、909、918 行 |
| C03 | 执行过程折叠组 | 用户输入与最终回答之间，将思考、工具过程汇总为展开条。 | 显示过程消息数、工具调用数；运行中尾部不收起；完成且有答案时默认折叠，无答案则展开。 | 对比在摘要条补充失败数、修改文件数等真实结果后，能否兼顾阅读与过程追溯。 | [ChatWindow](../../components/ChatWindow.tsx)，第 213、833、858 行 |
| C04 | 普通输入器 | 多行输入框和发送按钮；底栏放附件、模型、推理、工具预设、压缩、声音等控制。 | 无文字且无图片时禁用发送；桌面 Enter 发送、Shift+Enter 换行；移动端 Ctrl/Cmd+Enter 发送；支持草稿恢复。 | 将本次输入操作和会话配置分组，比较主发送操作的可发现性；桌面与移动端分别记录。 | [ChatInput](../../components/ChatInput.tsx)，第 437、1094、1915、2025、2062 行 |
| C05 | 图片附件 | 底栏图片按钮、粘贴、拖放；输入框上方展示缩略图和移除按钮；拖入时覆盖涟漪图形。 | 多图、单图删除、大图压缩、草稿图片恢复；图片存在时不进入 Bash 模式。 | 对比附件文件名、体积、处理状态，以及缩小拖放提示后的上下文可见度。 | [ChatInput](../../components/ChatInput.tsx)，第 239、259、1552、2073 行；[ChatWindow](../../components/ChatWindow.tsx)，第 628 行 |
| C06 | 运行中追加输入 | 流式期间，输入右侧出现 Steer 和 Follow-up；上方为排队消息区。 | 当前运行注入、完成后继续、队列数量及类别、召回；没有候选菜单和输入法组合态时，Esc 停止。 | 对比“立即补充”“完成后继续”等直观文案，使操作差异无需悬停即可理解。 | [ChatInput](../../components/ChatInput.tsx)，第 1026、1209、1425、1977 行 |
| C07 | 输入历史菜单 | 空输入按 ↑，菜单位于输入器上方。 | 上下选择，Tab/发送快捷键应用，Esc 关闭。 | 对比可见历史入口、时间或摘要，是否更容易找到相似历史输入。 | [ChatInput](../../components/ChatInput.tsx)，第 1106、1200、1573 行 |
| C08 | Slash 命令面板 | 输入 `/`；内建、扩展、提示词、技能按来源分组。 | 过滤、计数、加载、无匹配、方向键、Tab；内建 compact/reload/name/session/copy/clone；技能有休眠逻辑。 | 对比来源徽标、休眠原因、执行影响说明；区分执行动作与插入提示词。 | [ChatInput](../../components/ChatInput.tsx)，第 144、171、203、1130、1695 行 |
| C09 | @ 文件补全 | 输入 @ 查询文件，候选列表位于输入器上方。 | 加载、无匹配、选择、方向键、Tab/发送快捷键补全。 | 对比文件名主信息、路径次信息、目录范围与同名文件区分。 | [ChatInput](../../components/ChatInput.tsx)，第 1178、1819、1849 行 |
| C10 | Shell 输入模式 | `!` 开头进入 Shell，`!!` 为本地输出；输入下方有模式说明。 | `!` 输出进入模型上下文，`!!` 不进入；有图片时不进入该模式。 | 对比明确可退出的模式标签和输出范围说明，减少前缀差异被忽略。 | [ChatInput](../../components/ChatInput.tsx)，第 459、2055 行 |
| C11 | 模型选择弹层 | 输入底栏模型按钮；Provider 分组列表；组件支持不同外观变体。 | 过滤、当前选中、清除为默认（调用方提供时）、无模型、无匹配、切换中、锁定。 | 对比真实能力信息和无模型恢复入口；统一现有局部英文文案。 | [ModelSelector](../../components/ModelSelector.tsx)，第 48、163、231 行 |
| C12 | 推理级别与工具预设 | 输入底栏两个菜单；移动端可收入更多控制。 | 选中项高亮；流式期间不能切换；更多控制可展开和收起。 | 对比级别、预设影响的短说明及禁用原因，减少图标猜测。 | [ChatInput](../../components/ChatInput.tsx)，第 2127、2192、2280、2476 行 |
| C13 | 压缩、停止与声音 | 输入底栏；重试信息位于输入区上方。 | 闲置压缩，压缩中可停止压缩；运行中红色停止；声音开关；显示重试次数和原因。 | 对比稳定的停止位置、集中状态反馈、压缩结果说明和可执行恢复操作。 | [ChatInput](../../components/ChatInput.tsx)，第 1495、2364、2405、2432 行 |
| C14 | 用户消息 | 正文、图片、文件链接，底部操作和时间；长文本可折叠。 | 复制、从这里编辑、从此处分叉会话；忙碌时限制相关操作。 | 对比编辑历史与新建分支的明确标签、去向提示和长消息摘要。 | [MessageView](../../components/MessageView.tsx)，第 291、396、473、511、536 行 |
| C15 | Assistant 回答 | 上方模型名，正文纵向内容块，下方用量、时间、复制；流式显示估算 Token 和 t/s。 | Markdown、文件链接、错误、复制、速度、本轮写入文件。 | 对比正文优先级；将速度、用量放入详情的方案；区分速度色彩和错误语义。 | [MessageView](../../components/MessageView.tsx)，第 572、725、751、782、813 行 |
| C16 | 思考内容 | Assistant 内可折叠 Thinking 区块。 | 延迟加载、展开、加载中、错误、耗时。 | 对比过程与正式答复层次，以及失败后可重试的反馈。 | [MessageView](../../components/MessageView.tsx)，第 873、883、906 行 |
| C17 | 工具调用与结果 | 消息内折叠工具卡，展开查看输入与结果。 | 运行、成功、错误、耗时、文字/图片结果；子代理工具可打开关联会话。 | 对比统一动作、对象、结果、耗时摘要，长输入输出分段后是否容易扫描。 | [MessageView](../../components/MessageView.tsx)，第 961、990、1021、1308 行 |
| C18 | 工具 Diff | 识别为差异的结果，嵌入并排或 Patch 视图。 | 新旧文件头、增加/删除/上下文行。 | 对比并排/统一切换、固定文件头、长文件折叠；重点验证窄屏。 | [MessageView](../../components/MessageView.tsx)，第 1076、1091、1147、1230 行 |
| C19 | 压缩与自定义消息 | 消息流中的特殊消息容器。 | 压缩摘要含读取/修改文件；自定义消息可截断展开、复制等。 | 对比低干扰且可辨识的容器，以及压缩对后续会话影响的说明。 | [MessageView](../../components/MessageView.tsx)，第 1377、1429、1460 行 |
| C20 | Bash 执行消息 | 消息中的命令和输出块。 | 部分输出、查看完整输出、加载、失败；区分是否计入上下文。 | 对比命令、结果、退出状态层次；统一 view full output/loading 等英文文案。 | [MessageView](../../components/MessageView.tsx)，第 1703、1716、1756 行 |
| C21 | 本轮写入文件 | 最终回答下方，图标和文件名组成的小按钮。 | 打开预览；title 显示全路径；仅来自成功 write/edit 调用，不扫描答复路径；无文件隐藏。 | 对比同名文件最短可区分路径；文件多时可折叠列表。 | [TurnWrittenFiles](../../components/TurnWrittenFiles.tsx)，第 8、13 行；[ChatWindow](../../components/ChatWindow.tsx)，第 885 行 |
| C22 | 聊天 Minimap | 桌面右侧窄轨道，节点表示轮次；移动端隐藏。 | 点击/拖动定位；悬停显示各轮用户摘要、回答大纲；可跳用户、Assistant、标题。 | 对比显式大纲入口、键盘操作和移动端替代入口，降低对鼠标悬停的依赖。 | [ChatMinimap](../../components/ChatMinimap.tsx)，第 140、601、673 行；[ChatWindow](../../components/ChatWindow.tsx)，第 953 行 |
| C23 | 分支导航 | 顶部“分支”打开下拉树，支持独立条和紧凑按钮。 | 当前路径高亮、连续链压缩、点击切换；无会话/无分支空态；内容最多约 260px 高。 | 对比分支名、数量、分叉来源、切换结果提示；检查深层缩进。 | [BranchNavigator](../../components/BranchNavigator.tsx)，第 31、55、127、256、286 行 |
| C24 | Agent 会话切换 | 顶部 Agents 面板；根任务首项，随后子任务。 | 总数、运行数、状态、标题/描述、相对时间；运行优先排序；超过 8 个子任务显示搜索；点击切换。 | 对比稳定搜索入口、状态筛选、父子关系，区分正在查看与正在运行。 | [AppShell](../../components/AppShell.tsx)，第 2020 行；[AgentSessionPanel](../../components/AgentSessionPanel.tsx)，第 70、153 行 |
| C25 | 系统提示词查看 | 顶部 System 面板，只读等宽文本，保留换行。 | 滚动；未加载、加载中、空内容；最高 min(600px,75dvh)。 | 对比复制、搜索、章节导航；来源和更新时间只能采用真实数据。 | [AppShell](../../components/AppShell.tsx)，第 2029 行；[SystemPromptPanel](../../components/SystemPromptPanel.tsx)，第 9 行 |
| C26 | 工具定义查看 | 顶部 Tools 面板，左列表、右详情。 | 仅 active 工具；描述、参数数、类型、必填/可选、允许值、默认值、Guidelines；加载/空工具/无参数。 | 对比搜索、长名称截断、窄屏逐级详情、按需原始 Schema。 | [AppShell](../../components/AppShell.tsx)，第 2036 行；[ToolDefinitionsPanel](../../components/ToolDefinitionsPanel.tsx)，第 98、140 行 |
| C27 | 扩展状态条 | 输入器下方；无状态及 Widget 时隐藏。 | 按 key 排序拼接状态，支持 ANSI，整体 role=status。 | 对比按扩展分组、来源徽标、长状态展开，验证来源是否易识别。 | [ExtensionStatusBar](../../components/ExtensionStatusBar.tsx)，第 17、24 行 |
| C28 | 扩展 Widgets | 状态条中多个标签，展开内容位于标签上方。 | key、放置方向、更新脉冲；一次展开一个；空内容不可点击；ANSI。 | 对比可读名称（需元数据）、大量 Widget 收纳，减少内部 key 和位置语义负担。 | [ExtensionWidgets](../../components/ExtensionWidgets.tsx)，第 49、116、141 行 |
| C29 | 扩展交互弹窗 | 扩展请求时覆盖聊天区，居中，宽 min(560px,100%)。 | select、confirm、input、editor；选择、确认、取消；输入 Enter 提交/Esc 取消。 | 对比统一标题、说明、主次操作；长列表搜索；实际验证焦点管理和键盘闭环。 | [ChatWindow](../../components/ChatWindow.tsx)，第 660、1074、1098 行 |
| C30 | 扩展自定义 UI | 扩展发起终端式自定义面板。 | ANSI 行、聚焦输入、键盘事件传递、Ctrl-C 关闭。 | 对比明确的可输入说明、操作帮助和退出入口。 | [ChatWindow](../../components/ChatWindow.tsx)，第 1266、1282 行 |
| C31 | 通知和模型错误 | 右上通知；输入前置模型错误/Scope Warning；消息内 Provider Error。 | 类型颜色、悬停暂停通知；重试次数与原因。 | 对比统一错误层级和恢复动作；源码只能证明多个承载位置，不能据此断言重复报错。 | [ChatWindow](../../components/ChatWindow.tsx)，第 674、978 行；[ChatInput](../../components/ChatInput.tsx)，第 375、420 行；[MessageView](../../components/MessageView.tsx)，第 782 行 |

## 优化前后对比与验收建议

1. **输入与运行控制（C04/C06）**：固定同一会话，记录闲置、流式生成、排队两条消息、停止、模型错误和移动端更多菜单六组状态。验收发送、追加、停止的作用是否无需悬停即可理解；不得在比较中改变会话内容来掩盖布局差异。
2. **消息阅读层次（C02/C03/C15/C17）**：使用包含工具、思考、最终答案、文件结果的同一轮会话，记录运行中、完成后折叠、过程展开。比较最终答案可见面积，以及找回指定工具结果所需操作数；具体数值应实测。
3. **会话导航（C22/C23/C24）**：分别建立长对话、多分支、多 Agent 样本。验证用户能否识别当前会话、当前分支、运行中的 Agent，并完成定位；桌面和移动端独立比较。
4. **开发信息面板（C25/C26）**：覆盖长系统提示词、多工具、复杂参数、未加载、加载中和空状态。比较可读行长、查找目标信息的操作数、窄屏布局与滚动范围。
5. **扩展界面（C27–C30）**：覆盖无扩展、多个 Widget、长状态、四种原生弹窗、自定义面板。没有实际扩展请求的动态状态应明确标为“源码确认，未取得运行截图”，不得用伪造数据截图冒充生产实况。
6. **错误反馈（C31）**：用可控的真实错误复现样本，检查错误归属、恢复入口与通知消失行为。对比时保留相同失败原因，不能只展示正常状态。

建议先处理输入与运行行为、消息阅读层次，再处理导航及信息面板，最后统一扩展和错误反馈。这是基于功能重要性的设计建议，不代表已通过用户研究或运行测评证明优先级。

## 记录边界

- 此文件仅记录现状、源码入口和对比建议，不包含界面改造实现。
- 源码行号对应记录时的工作区，后续修改可能移动；文件链接用于定位，行号以文本保留。
- 当前未逐态截图，也未执行浏览器交互验证；动画、焦点闭环、触控体验、实际溢出和运行错误均需后续实测。
