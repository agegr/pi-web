/**
 * Maximum width of the chat column's content — the message list, the empty-new
 * welcome card, and the composer, which must stay aligned with each other.
 *
 * `"100%"` lets the content fill the panel and grow with the window instead of
 * sitting in a fixed centered column. Set a pixel value here to reintroduce a
 * readable-line cap in one place.
 */
export const CHAT_CONTENT_MAX_WIDTH: string = "100%";
